#pragma once#include <iostream>using namespace std;#include "Identity.h"#include <string>#include <fstream>#include "globalFile.h"#include <vector>#include "student.h"#include "teacher.h"#include <algorithm>#include "computerRoom.h"class Manager :public Identity{public:    //default constructor    Manager();    Manager(string name ,string pwd);    //operator menu    virtual void operMenu();    //add account    void addPerson();    //show account    void showPerson();    //show information of the computer room    void showComputer();    //clear appointment records    void cleanFile();       void initVector();    //check duplication id    bool checkRepeat(int id , int type);    vector<Student>vStu;    vector<Teacher>vTea;    vector<ComputerRoom>vCom;};