#pragma once#include <iostream>using namespace std;#include "globalFile.h"#include <fstream>#include <map>#include <string>class OrderFile{public:    OrderFile();    //Update appointment record    void updateOrder();    //the number of records    int m_Size;    //record all information,    //the key represents the number of records, and the value represents the specific record information    map<int, map<string, string>> m_orderData;};